---
title: Portfolio
layout: engage
curr_page: portfolio
---

section#portfolio-section
  - data.portfolio_splash.each do |project|
    = partial "partials/portfolio_splash",
      :locals => { \
        :title => project.title,
        :body => project.body,
        :url_path => project.url_path,
        :img => project.img,
      }

section#blog-section
  = partial "partials/blog_feed"
