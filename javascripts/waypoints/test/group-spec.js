"use strict";window.jQuery.each(Waypoint.adapters,function(t,e){describe(e.name+" adapter:",function(){describe("Waypoint.Group",function(){var t,n,i,o,r=window.jQuery;beforeEach(function(){Waypoint.Adapter=e.Adapter,loadFixtures("standard.html"),t=new Waypoint({element:r("#same1")[0],handler:function(){}}),n=new Waypoint({element:r("#near1")[0],handler:function(){}}),i=new Waypoint({element:r("#same2")[0],group:"custom",handler:function(){}}),o=t.group}),afterEach(function(){t.destroy(),n.destroy(),i.destroy()}),describe("#previous(waypoint)",function(){it("returns previous waypoint based on trigger point",function(){expect(o.previous(n)).toEqual(t)}),it("returns null if on the first waypoint in the group",function(){expect(o.previous(t)).toBeNull()})}),describe("#next(waypoint)",function(){it("returns next waypoint based on trigger point",function(){expect(o.next(t)).toEqual(n)}),it("returns null if on the last waypoint in the group",function(){expect(o.next(n)).toBeNull()})}),describe("#first()",function(){it("returns the first waypoint based on trigger point",function(){expect(o.first()).toEqual(t)})}),describe("#last()",function(){it("returns the first waypoint based on trigger point",function(){expect(o.last()).toEqual(n)})})})})});